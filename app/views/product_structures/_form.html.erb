<%= form_with(model: product_structure, local: true) do |form| %>
  <% if product_structure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_structure.errors.count, "error") %> prohibited this product_structure from being saved:</h2>

      <ul>
      <% product_structure.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
