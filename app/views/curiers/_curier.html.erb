<div class="col-md-12 col-sm-12 col-xs-12">
  <div class="x_panel">
    <div class="x_title">
      <h2>Kurier <small> aktualne ceny</small></h2>
      <ul class="nav navbar-right panel_toolbox">
        <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
        </li>
        <li><a class="close-link"><i class="fa fa-close"></i></a>
        </li>
      </ul>
      <div class="clearfix"></div>
    </div>
    <div class="x_content">
      <p class="text-muted font-13 m-b-30">
        <%= link_to("Create New", new_curier_path, class: "btn btn-success") %>  </p>
      <table id="datatable-buttons" class="table table-striped table-bordered">
        <thead>
        <tr>
          <th>Nazwa</th>
          <th>Wymagania</th>
          <th>Maksymalna Wysokość</th>
          <th>Cena</th>
          <th class="col-md-2 center"><em class="fa fa-cog"></em></th>
        </tr>
        </thead>
        <tbody>
        <% obj.each do |curier| %>
            <tr>
              <td><%= curier.label %></td>
              <td><%= curier.maxweight %>kg - <%= curier.minweight %>kg</td>
              <td><%= curier.maxheight %>m</td>
              <td><%= curier.price %> zł</td>
              <td>
                <%= link_to(curier_path(curier), class: 'glyphicon glyphicon-search') do %>
                <% end %>
                <%= link_to(edit_curier_path(curier), class: 'glyphicon glyphicon-edit') do %>
                <% end %>
              </td>
            </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>



