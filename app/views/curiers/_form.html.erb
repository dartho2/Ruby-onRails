<%= form_with(model: curier, local: true) do |form| %>
  <% if curier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(curier.errors.count, "error") %> prohibited this curier from being saved:</h2>

      <ul>
      <% curier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
