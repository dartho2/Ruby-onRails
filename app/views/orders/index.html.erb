<div class="container">
  <div class="row">
    <div class="col-md-10 col-md-offset-1">
      <div class="panel panel-default panel-table">
        <div class="panel-heading">
          <div class="row">
            <div class="col col-xs-6">
              <h3 class="panel-title text-right">Listing Orders</h3>
            </div>
            <div class="col col-xs-6 text-right">
                <%= link_to("Create New", new_product_path, class: "btn btn-success") %>
              </div>
            </div>
          </div>

            <div class="panel-body">
              <table class="table table-striped table-bordered table-list">
                <thead>
                <tr>
                  <th class="col-sm-3">Quantity</th>
                  <th class="col-sm-3">Name Product</th>
                  <th class="col-sm-2">Price</th>
                  <th class="col-sm-2 center"><em class="fa fa-cog"></em></th>
                </tr>
                </thead>
                <% @orders.each do |order| %>
                    <tr data-toggle="collapse" data-target="#<%= order.id %>" class="accordion-toggle">
                      <td>
                        <span class="glyphicon glyphicon-plus"></span>
                      </td>
                      <td><%= order.name %></td>
                      <td><%= order.total_price %></td>
                      <td class="center">
                        <%= link_to "Delete", order, method: :delete, class: 'btn btn-danger' %></i>
                        <%= link_to "Edit", edit_order_path(order), class: 'btn btn-warning' %>
                      </td>
                    </tr>
                    <tr class="hiddenRow">
                      <td colspan="12" class="hiddenRow">
                        <div class="accordian-body collapse" id="<%= order.id %>" href="#collapseThree">
                          <table class="table table-striped">
                            <thead>
                            <th>Name</th>
                            <th>Quantity</th>
                            <th>price</th>
                            </thead>
                            <tbody>
                            <% order.products.each do |order, array| %>
                                <tr>
                                  <td><%= order.name %></td>
                                  <td><%= order.product_orders.map(&:quantity).join(', ') %></td>
                                  <td><%= order.price %></td>
                                </tr>
                            <% end %>
                            <tr>
                              <td></td>
                              <td></td>
                              <td><%= order.total_price %> zł</td>
                            </tr>
                            </tbody>
                          </table>
                        </div>
                      </td>
                    </tr>
                <% end %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>


