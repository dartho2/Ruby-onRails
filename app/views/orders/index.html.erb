<div class="col-md-12 col-sm-12 col-xs-12">
  <div class="x_panel">
    <div class="x_title">
      <h2>Faktury <small> -</small></h2>
      <ul class="nav navbar-right panel_toolbox">
        <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-wrench"></i></a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="#">Settings 1</a>
            </li>
            <li><a href="#">Settings 2</a>
            </li>
          </ul>
        </li>
        <li><a class="close-link"><i class="fa fa-close"></i></a>
        </li>
      </ul>
      <div class="clearfix"></div>
    </div>
    <div class="x_content">
      <p class="text-muted font-13 m-b-30">
       </p>
      <table id="datatable-buttons" class="table table-striped table-bordered">
        <thead>
        <tr>
          <th>Numer FV</th>
          <th>Cena</th>
          <th>Akcja</th>

        </tr>
        </thead>


        <tbody>
        <% @orders.each do |order| %>
            <tr data-toggle="collapse" data-target="#<%= order.id %>" class="accordion-toggle">
              <td><%= order.name %></td>
              <td><%= order.total_price %></td>
              <td class="center">
                <%= link_to "Zobacz", order_path(order), class: 'btn btn-warning' %>
                <%= link_to "Delete", order, method: :delete, class: 'btn btn-danger' %></i>
                <%= link_to "Edit", edit_order_path(order), class: 'btn btn-warning' %>
              </td>
            </tr>

        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>







